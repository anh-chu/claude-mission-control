node_modules/
.next/
.env*
!.env.example
CLAUDE.local.md
*.log
dist/
out/
coverage/
.vercel/
tsconfig.tsbuildinfo

# Claude personal settings (not shared)
.claude/settings.local.json
.claude/projects/
.claude/plans/
.claude/launch.json

# Generated AI context (regenerated by script)
data/ai-context.md
.codesight/

# Mission Control PID file (generated by start/stop scripts)
.mc.pid

# Daemon runtime state (regenerated at runtime, may contain error details)
data/daemon.pid
data/daemon-status.json
data/active-runs.json
data/respond-runs.json

# llm-wiki-pm runtime artifacts (live in repo root when wiki dir is colocated)
.wiki-lock
_status.md
queries/

# Local package tarballs
*.tgz

# Checkpoint data (user checkpoints not pushed to repo, demo included)
data/checkpoints/*.json
!data/checkpoints/snap_demo.json

# Cowork session mounts
/sessions/

# Field Ops (private — credentials, temp configs, attachments)
data/field-ops/.credentials.json
data/field-ops/.tmp/
data/field-ops/attachments/

# Field Ops operational data (live state, not source)
data/field-ops/activity-log.json
data/field-ops/missions.json
data/field-ops/approval-config.json

# Personal data files (use `pnpm seed:demo` to generate sample data)
data/tasks.json
data/tasks-archive.json
data/goals.json
data/projects.json
data/brain-dump.json
data/activity-log.json
data/inbox.json
data/decisions.json
data/missions.json

# Research & marketing files (local only, not pushed to GitHub)
research/
**/.omc/artifacts
**/.omc/plans
**/.omc/state
**/.omc/sessions
**/.omc/prd.json
**/.omc/progress.txt
**/state
**/.omc/artifacts
**/.omx/

# Test data
/.test-data/

# Agent scratch / tooling local state
.pi/
.pi-lens/
babysitter-diagnostic-report.html
dist/
bin/cli.js
